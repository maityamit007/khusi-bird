import { saveSystem } from "./save";

export async function makeScoreBox(kaboom, postMessage, score){
//   await saveSystem.load();

//   if (score > saveSystem.data.maxScore) {
//     saveSystem.data.maxScore = score;
//     await saveSystem.save();
//   }
}